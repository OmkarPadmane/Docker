make folder in system in that :
  -Docker and index.html files
in terminal :
  -cd that folder:
  create image:
    - sudo docker build -t m2 .
  create container:
    - sudo docker run --name z444  -p 80:80 -it a4
in new terminal:
    - curl localhost:80
